import { ArrayComasPipe } from './array-comas.pipe';

describe('ArrayComasPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayComasPipe();
    expect(pipe).toBeTruthy();
  });
});
